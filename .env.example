# Example environment configuration for evnt-hndlr
# Copy this file to .env and update with your actual values

# GitHub repository URL (HTTPS format)
ASTRO_REPO_URL=https://github.com/yourusername/your-astro-site.git

# Local path where the repository will be cloned/updated
# This can be a temporary directory
LOCAL_REPO_PATH=/tmp/astro-site

# GitHub personal access token
# Create one at: https://github.com/settings/tokens
# Required permissions: repo (full control of private repositories)
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: Override default branch (defaults to 'main')
# TARGET_BRANCH=main
